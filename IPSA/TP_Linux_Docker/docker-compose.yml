version: '3'

services:
  in322:
    build:
      context: .
      dockerfile: Dockerfile
    tty: true
    stdin_open: true
    volumes:
      - /home/florianandre/Documents/School-codes/IPSA/TP_Linux_Docker:/bin223
    command: bash

