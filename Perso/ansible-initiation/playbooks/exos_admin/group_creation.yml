- name: group creation
  hosts: localhost
  become: true
  vars:
    system_groups:
      - name: group1
      - name: group2
      - name: group3
  tasks:
    - name: Add multiple groups
      ansible.builtin.group:
        name: "{{ item.name }}"
        state: present
      loop: "{{ system_groups }}"